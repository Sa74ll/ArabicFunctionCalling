name: bootcamp_project
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    # HuggingFace ecosystem
    - datasets>=2.16.0
    - transformers>=4.36.0
    - huggingface_hub>=0.20.0
    # Data processing
    - pandas>=2.1.0
    - jsonlines>=4.0.0
    - tqdm>=4.66.0
    # Validation & testing
    - jsonschema>=4.20.0
    # For Phase 2-4 (fine-tuning)
    - torch>=2.1.0
    - accelerate>=0.25.0
    - bitsandbytes>=0.41.0
    - peft>=0.7.0
    - unsloth>=2024.1
    # Utilities
    - python-dotenv>=1.0.0
    - rich>=13.7.0
